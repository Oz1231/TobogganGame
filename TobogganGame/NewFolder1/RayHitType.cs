namespace TobogganGame
{
    /// <summary>
    /// Type of object hit by a ray
    /// </summary>
    public enum RayHitType
    {
        None,
        Wall,
        Body,
        Flag,
        Obstacle
    }
}